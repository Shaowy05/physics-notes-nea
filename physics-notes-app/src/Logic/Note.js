export default class Note {

    constructor(id, path, name, authorId, parentFolderId) {
        this.id = id;
        this.path = path,
        this.name = name,
        this.authorId = authorId,
        this.parentFolderId = parentFolderId
    }

}